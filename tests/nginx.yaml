apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: nginx
    tier: frontend
spec:
  nodeName: k8s-worker01.kubernetes.lan
  containers:
    - name: nginx-container
      image: nginx

